{"id": "tierD-000208", "run": 90,
 "q1_faithful": "partial",
 "q1_issues": [{"statement": 6, "issue": "\"near foreign compounds\" answers *where* for the staging event, and the instruction set routes an event PP answering where/when/how to the named role `Location`, reserving the preposition-named oblique for PPs naming what the activity is about or directed at (the surface-preposition rule that keeps `Near`/`NextTo` is stated for entity-attached position, not for events)."}],
 "q2_coverage": "gap",
 "q2_gaps": ["a non-containment locative preposition (\"near\") modifying an EVENT: the instruction set fixes surface-head retention only for entity-attached position and lists `Location` for event where-PPs, so proximity prepositions on events are unsettled", "past-tense hedging *seem* (\"seemed similar\"): the epistemic route is specified, but whether the clause's own past marking survives beside `Probably` is not stated"],
 "q3_context_leak": "na",
 "q3_leaks": [],
 "q4_unlicensed_heads": [],
 "notes": "Everything else follows the instruction set: `Similar` + `(Symmetric Similar)` for the stative symmetric predicate, epistemic `Probably` at confidence 0.9 for \"seemed\", perfect \"have blamed\" collapsed to `Past`, the ground-\"for\" of *blame* as the `(For <event> …)` oblique, `foreign_compound` fused with genus plus adjectival modifier genus, and `LocatedIn` for the containment \"in Riyadh\". The `Near` flag is a role-routing question, not a coined head."}
