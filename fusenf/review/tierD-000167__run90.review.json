{"id": "tierD-000167", "run": 90,
 "q1_faithful": "partial",
 "q1_issues": [{"statement": 4, "issue": "\"remained in stable condition\" mints a matrix `remain` event, but the intransitive continuation copular (\"remained/stayed X\") explicitly reifies the state with `(Still ...)` + the flat atom + tense and mints NO matrix event; the locative-PP branch likewise routes to the entity-attached position atom, not a `remain` event."},
            {"statement": null, "issue": "No `(Still ...)` atom anywhere, so the continuation sense of \"remained\" is dropped — the parse says only that she was in stable condition on Saturday."},
            {"statement": null, "issue": "No flat / dual-emit atom ties bethany_hamilton to the stable-condition state, so \"is she in stable condition?\" binds nothing at top level; the only link is the `In` oblique on the invented event."},
            {"statement": 3, "issue": "Mints a witness `sk_condition_1` for the bare, determinerless \"stable condition\"; an oblique's object NP with no article stays a bare constant."}],
 "q2_coverage": "gap",
 "q2_gaps": ["continuation copular (`remain`/`stay`) with a non-locative predicative PP complement (\"remained in stable condition\") — the section covers only an adjectival complement (state + `Still` + flat) and a locative PP (position atom, continuation gap), and neither shape fits a predicative `in`-PP"],
 "q3_context_leak": "na",
 "q3_leaks": [],
 "q4_unlicensed_heads": [],
 "notes": "The \"after the attack Friday morning\" half is well handled: anchor event-noun typed, `Before` argument order correct for \"after\", and the stated Friday/morning terms are traceable to the words. Fusing `stable_condition` as a term of art with genus `condition` + gradable-modifier genus `stable` is licensed."}
