{"id": "tierD-000221", "run": 90,
 "q1_faithful": "partial",
 "q1_issues": [{"statement": 3, "issue": "`(Inheritance industrial_average industrial)` exposes a relational/classifying adjective that fails the predication test (*\"the average is industrial\" — it is an average of industrials), which *Compound concepts* bars; only the `average` genus is licensed."},
               {"statement": 6, "issue": "A scalar-change subject takes Theme, not Patient (*Thematic roles*: \"a scalar-change subject rides *Scalar change* and is likewise Theme\", as in the fall example)."},
               {"statement": 8, "issue": "The closing level \"at 9,317.64\" is the change's end value and belongs on MeasureTo on the eventuality, and the scale `value` is supplied by no word in the sentence — an unstated dimension takes the reserved `unspecified`, never an invented one (the unit `point` is likewise carried over from the delta phrase)."},
               {"statement": 9, "issue": "A stated delta rides `MeasureBy`, not a plain `Measure` on the event (the only event-borne plain Measure the instruction set licenses is `duration`); the scale slot also holds the direction word `down` rather than a stated dimension."},
               {"statement": 10, "issue": "Same as statement 9 for the percentage restatement — the head should be MeasureBy on the change eventuality."}],
 "q2_coverage": "gap",
 "q2_gaps": ["\"finished down N\" — a culmination verb plus a directional particle: *Scalar change* names change-of-value verbs (rise/fall/...) and the copular \"was up/down\" state, but not this pattern, so which eventuality carries the figures and where the \"down\" direction is recorded is unstated"],
 "q3_context_leak": "na",
 "q3_leaks": [],
 "q4_unlicensed_heads": [],
 "notes": "Name handling is exactly right — \"Dow Jones\" as the capitalized run, the lowercase kind in its own type atom, and the .DJI ticker correctly dropped — and the \"or 0.33 percent\" restatement is correctly two atoms with no Or. The quantitative layer is where it fails: wrong subject role and every figure on a plain Measure with one invented scale."}
